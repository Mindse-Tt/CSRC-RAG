%%{init: {"theme":"dark"}}%%
xychart-beta
    title "Recall@5 四档检索消融对比（gold_50 人工评测 n=38，2026-04-22 M3d）"
    x-axis ["BM25-only", "Dense-only (bge)", "Hybrid (RRF)", "+Rerank (bge-v2-m3)"]
    y-axis "Recall@5" 0 --> 0.35
    bar [0.1140, 0.0680, 0.1557, 0.0855]
```

> **说明**：M3d 里程碑最终数值（2026-04-22 23:40，ExecutionAgent-M3d 写入）。条形图来自 `docs/reports/m3_retrieval_report.json`（n=38，gold=`data/eval/gold_50.jsonl` 人工标注 `relevant_event_ids`，多 gold 语义 `Recall@5 = |top5 ∩ gold| / |gold|`）。
>
> **M2 → M3d 主要改动**：
> 1. **R1**（M2）中文 encoder：`all-MiniLM-L6-v2` → `BAAI/bge-small-zh-v1.5`。
> 2. **R2**（M3c）metadata 硬过滤 → 软过滤（置信度 ≥ 0.7 才硬过滤，< 0.7 转 boost hint；候选池 < 20 自动回退整库）。
> 3. **R3**（M3c）tokenizer：正则 bigram → `jieba` + 停用词 + `synonyms.json` canonical 作 user_dict。
> 4. **R4**（M3c）BM25/Dense 候选池 top-50 → top-100，chunk→event 聚合池扩到 100。
> 5. **M3d** rerank 合并层 bug 修复：rerank 从"替换 hybrid top-k"改为"与 hybrid rank 做 RRF 融合"，消除 top-k 截断导致的 gold 丢失。
>
> **数据解读**：
> 1. Hybrid 最佳 0.1557，相对 M2 同配置自举值（0.0733）**翻倍**，证明 R2/R3 软过滤 + jieba 对跨案例评测集有显著增益。
> 2. M3c Hybrid+Rerank 回归到 0.0526，M3d 修复合并 bug 后恢复到 **0.0855 (+62%)**；未能超过 Hybrid 0.1557 是语义层瓶颈：bge-reranker 未在 CSRC 领域适配，对"带年份/身份硬约束的 query"排序力弱。
> 3. 距目标 BM25 Recall@5 ≥ 0.35 / Hybrid+Rerank ≥ 0.60 仍有 3-7× 差距，已识别三条后续路径（领域 LoRA 重排、listwise 重排、query 改写），不在 M3 范围。
>
> 论文 Ch4.2 Fig.20 引用此图说明 **"中文 embedding + jieba + 软过滤 + 候选池扩充"四件套对跨案例 gold 的增益可观，但单纯 cross-encoder 不经领域适配在硬约束 query 上反成噪声 —— 重排的正确集成应是 RRF 融合而非硬替换**。
