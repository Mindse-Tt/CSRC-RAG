# 一页纸决策清单（Coordinator 给用户 + 组员的行动清单）

> 合稿时间：2026-04-22 · D0 · 配套《策略总览.md》阅读
> 读我花 3 分钟，执行可以救 3 天

---

## A · 立即要用户拍板的 1 件事 ⚠️

> 已拍板 2 件：①显卡=本机 RTX 2060 SUPER 8GB（Turing fp16，不支持 bf16）；②数据口径=14,740→4,233 事件+14,740 当事人，"8,000"口径废弃。

**剩下 1 件**：
1. **D6 线下 2h 全员标注评测集（50 条金标）的时间点** —— 参考甘特是 2026-04-27（周一）晚 19:00–21:00。⚠️ 若冲突请**今晚定**，否则 M3 里程碑（D6）全员阻塞、后续训练期顺延。

---

## B · Coordinator 未来 48h 的 5 件事（可命令级执行）

1. **Patch 08 文档的 bf16 → fp16**（Turing 不支持 bf16）
   ```bash
   # 把 08-response-training-strategy.md 里 bnb_4bit_compute_dtype=bfloat16 改 float16
   # 把 per_device_batch 4 → 2，grad_accum 4 → 8（有效 batch 仍 16）
   ```
2. **创建 5 个 feature 分支**
   ```bash
   gh api repos/Mindse-Tt/Deeplearning-Rag-Test -X POST -f name=feat/intent-v2-xu
   # feat/bge-index-jia / feat/qa-data-dai / feat/prompt-zhang / feat/eval-wang
   ```
3. **准备 RTX 2060S 本机训练包**
   - 验证 `nvidia-smi` 能看到 2060S 8GB
   - `pip install -q peft==0.11.* transformers==4.44.* bitsandbytes==0.43.* accelerate==0.33.* trl==0.9.*`
   - 让王怡菲 D1 先跑 200 条样本 smoke test（1 epoch ~ 10 min），验证不 OOM
4. **抽 100 条候选金标题目**
   ```bash
   python -c "import json; ..." # 从 event_corpus.jsonl 按 4 类意图分层抽 100 条 → data/eval/gold_50_candidates.jsonl
   ```
5. **建每日 20min 站会** + 推送今日决策给组员（GitHub Issue #1 钉置顶 + 微信群）

---

## C · 本周 5 位组员每人 3 个动作（D1-D2 硬动作）

### 许浩财（架构/意图/集成）
1. `configs/intents_v2.json` 定稿 7 类 + case_retrieval 3 子意图（今晚前）
2. 跑 `scripts/train_intent_classifier_v2.py --backend sklearn`，Macro-F1≥0.92
3. 起草 `src/csrc_rag/orchestration/planner.py` 骨架输出 `QueryPlan`

### 贾彤（数据/向量库）
1. `scripts/split_by_event.py` 跑完，产出 `data/processed/splits/{train,val,test}.event_ids.txt`
2. 下载 `BAAI/bge-small-zh-v1.5`（走 `HF_ENDPOINT=https://hf-mirror.com`），跑 `scripts/build_dense_index_bge.py` → `chunk_embeddings_bge.npy`
3. 整理 `configs/synonyms.json` 至少 200 对（60 条样例模板见 `03` 文档附录 A）

### 戴一鑫（QA/拒答）
1. `configs/reject_templates.json` 7 个话术模板入库
2. 写 300 条拒答负样本 + 100 条 H1 反幻觉负样本（"诱导引用不存在法条"）
3. 起 `src/csrc_rag/orchestration/reject_policy.py`（接收 topic_guard 结果→4 级降级）

### 张彦扬（QA/Prompt）
1. `configs/prompt_templates/{case_retrieval,law_grounding,sanction_recommendation,trend_analysis}.j2` 定稿
2. 写强证据 Prompt 宽/中/严三档（见 07 §4.2 System Prompt）
3. 基于 `event_corpus.jsonl` 先试写 100 条 B 类（法条依据）样本，跑通模板

### 王怡菲（模型/评估/LoRA）
1. `configs/models.recommended.json` 定稿并合并到 `configs/models.json`
2. **验证 RTX 2060S 能跑 QLoRA fp16**：200 条样本 smoke test，观察显存峰值 < 7GB
3. 实现 `scripts/evaluate_retrieval_sanity.py` 的 L1 组件指标（Recall@5/MRR/nDCG@10）

---

## D · "不做"清单（答辩老师若问，我们主动说"这不在 scope"）

- ❌ **不做开放聊天**：chitchat 一律走模板 + 引导回证监会话题
- ❌ **不做罚款金额回归预测**：聚类 PunishmentType 够用，金额回归易翻车
- ❌ **不做完整处罚文书生成**：只生成"推荐方式 + 代表案例 + 免责声明"结构化回复
- ❌ **不做银保监/外管局数据**：数据源只有证监会，保险类走软越界话术
- ❌ **不做完整 DST（Dialog State Tracking）**：只做轻量 `last_intent + last_entities` 滑窗
- ❌ **不做 7B+ 基座**：锁死 Qwen2.5-1.5B；3B/7B 作为 `experiments.alternate_generator` 可选消融
- ❌ **不做强制 Docker 化**：Dockerfile 放 `deploy/` 作为加分项，不进主线
- ❌ **不做 v2 趋势分析**（多维度折线）：V1 只出柱状图 + 表格
- ❌ **不做 LoRA DPO**：SFT 主线，DPO 风险高、样本不够

---

## E · 答辩前必须产出的 8 个 Artifact

| # | 交付物 | 文件路径 | 负责人 | 截止 |
|---|--------|----------|--------|------|
| 1 | **8–12 页论文 PDF** | `docs/paper/csrc_rag_v1.pdf`（含 Team Contributions + **AI Contribution Statement**） | 全员分章 | D13 |
| 2 | **≤15 页答辩 PPT** | `docs/pptx/defense_v1.pptx` | 许浩财 + 张彦扬 | D14 |
| 3 | **Demo 口播脚本**（4 幕，见 11-engineering §5） | `docs/demo/demo_script.md` | 许浩财 | D14 |
| 4 | **可复现 GitHub 仓** | `Mindse-Tt/Deeplearning-Rag-Test` 打 `v1.0` tag | 许浩财 | D14 |
| 5 | **一键启动脚本** | `start.bat`（见 11 §7.2）+ README 截图 | 许浩财 | D13 |
| 6 | **评测报告 JSON** | `artifacts/eval/{component_*.json, generation_matrix.json, end_to_end_scores.json}` | 王怡菲 | D12 |
| 7 | **7 组消融报告** | `artifacts/eval/ablation_table.csv` + `docs/reports/eval_figs/*.png` | 王怡菲 + 许浩财 | D12 |
| 8 | **AI Contribution Statement 专章** | 论文 §7（记录：用 Claude 做了策略合稿 / Opus 生成策略文档 / DeepSeek 辅助造 QA 等） | 全员 | D13 |

---

## F · 用户阅读顺序推荐

1. **先读本文（一页纸决策清单）** —— 5 分钟，看 §A 拍板 + §C 看组员今明两天做什么
2. **再读《策略总览.md》** —— 15 分钟，重点看 §4 技术栈 / §6 冲突裁决 / §8 分工表 / §10 开题口径差异
3. **按需深入 12 份原始策略** —— 只在冲突/怀疑时回看 `docs/strategies/0x-*.md`（推荐优先读：05 检索 / 08 LoRA / 09 评估）
4. **99-leader-orchestration.md 不用再读** —— 本合稿已吸收其 11 条冲突裁决 + 6 关里程碑 + 风险登记册

---

> ⚠️ **唯一阻塞**：D6（约 4/27 晚）50 条金标集标注时间，请用户今晚拍板。其他 48h 内由 Coordinator 自动推进。
